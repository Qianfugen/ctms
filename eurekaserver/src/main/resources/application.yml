#eureka 服务器端口号
server:
  port: 8050
#eureka相关的配置
eureka:
  instance:
    #主机名称
    hostname: localhost
  client:
    #eureka不对外提供接口
    register-with-eureka: false
    #是否拉取服务信息,用于注册中心集群
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8050/eureka/



